export declare class ColorPickerWidget {
    private setKeyboardControlActive;
    private isKeyboardControlActive;
    private focusHtmlInput;
    private onColorChanged;
    private hueSlider;
    private saturationSlider;
    private valueSlider;
    private colorPreview;
    private canvas;
    private canvasContainer;
    private saturationValueHandle;
    private hsv;
    private animFrameRequest;
    constructor(setKeyboardControlActive: (active: boolean) => void, isKeyboardControlActive: () => boolean, focusHtmlInput: () => void, onColorChanged: (hsv: number[]) => void);
    createDom(container: HTMLDivElement, hsv: number[]): void;
    updateColorChannel(format: string, channel: number, value: number): number[];
    protected updateBackgroundColor(color: string): void;
    dispose(): void;
    focusSlider(): void;
    private sliderPointerdownListener;
    private sliderKeydownListener;
    private sliderBlurListener;
    private addEventListeners;
    private removeEventListeners;
}
export declare function createSliderGradient(): string;
